#include <cstdio>
#include <vector>
using namespace std;

vector<int> v;

int main()
{
    int t;
    scanf("%d", &t);

    int n, m, num;
    while(t--) {
        scanf("%d", &n);
        for (int i=0; i<n; i++) {
            scanf("%d", &num);
            v.push_back(num);
        }

        scanf("%d", &m);
        while(m--) {

        }
    }
    return 0;
}
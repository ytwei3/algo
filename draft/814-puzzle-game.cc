//
/* 814 - Puzzle Game */
//
#include <stdio.h>

int n, m, q, board[1001][1001];

int main()
{
    while ( ~scanf("%d%d%d", &n, &m, &q) )
    {
        for (int i=0; i<n; i++)
            for (int j=0; j<n; j++)
                scanf("%d", &board[i][j]);

        while ( q-- )
        {
            scanf("%d%d%d", &);
        }

    }
    return 0;
}

//
// My scratching file!!
//
#include <stdio.h>
#include <iostream>
#include <iomanip>
#include <array>
#include <string>
#include <list>
#include <stack>
#include <queue>
#include <vector>
#include <unordered_map>
#include <algorithm>
#include <math.h>
#define endl "\n"
using namespace std;

int main()
{
    //ios::sync_with_stdio(false), cin.tie(0);

   // char str[101];
   // int t = scanf("%[^\n]", str);
   // getchar();
   // printf("%d", t);
    
    char s1[100], s2[100];
    scanf("%[^\n] ", s1);
    scanf("%[^\n] ", s2);
    printf("%s\n", s1);
    printf("%s\n", s2);
    return 0;
}

// file used for scratching!
//#include <bits/stdc++.h>
#include <stdio.h>
#include <vector>
#include <unordered_map>
#include <list>
#include <string>
int t, n, sum;

using namespace std;

bool func(int a, int b)
{
    return ( a < b );
}

int main ()
{
    long long a = 1;
    vector<int> v;
    v.push_back(1);
    v.push_back(1);

    printf("%lld\n", a - v.size());


    return 0;
}

// 439 - String Partition
//
//
#include <stdio.h>

char ar[203];

int main ()
{
    int n;
    while ( scanf("%d", &n) != EOF )
    {
        while ( n-- )
            printf("%d\n", n);
    }

    return 0;
}

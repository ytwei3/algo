//
// 145 Arctic Network
//
#include <stdio.h>
#include <vector>

struct Crood
{
    int x, y;
};

int t, n, s, p;
//int const INF = 0x3f3f3f3f;
double res;

int main()
{
    while( ~scanf("%d", &t) )
    {
        while ( t-- )
        {
            scanf("%d%d", &s, &p);
            for (int i=0; i<p; ++i)
                scanf("");
        }
    }
    return 0;
}


//
/* 376 - pigs */
//
#include <cstdio>
#define maxn 100
#define maxm 1000

int n, res;
int vis[maxn+1];

int dinic()
{
    return 1;
}

int main ()
{
    int m, cnt;
    while ( ~scanf("%d%d", &m, &n) )
    {
        for (int i=1; i<=m; i++)
            scanf("%d", &cap[i]);

        for (int i=1; i<=m; i++)
        {
            scanf("%d", &cnt);
            while ( cnt-- )
            {
            }
        }
        printf("%d", dinic());
    }
    return 0;
}


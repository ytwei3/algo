// 862 - Card Game
//
//
#include <stdio.h>
#include <queue>

std::queue<long long> q;

int main()
{
    long long n, m, l, s, t;
    while (true)
    {
        scanf("%lld%lld%lld%lld%lld", &n, &m, &l, &s, &t);
        if ( n == -1 && m == -1 && l == -1 && s == -1 && t == -1 )
            break;

        for (int i=0; i<n; ++i)
        {
        }
    }
    return 0;
}

scanf();
success 1, return 1;
success 2, return 2;
EOF, return -1;
fail, return 0;
success 1, fail at 2? return 1;

scanf("[^\n]", str), getchar();
use getchar to change line
this can scan with space

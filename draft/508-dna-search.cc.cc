//
// 508 - DNA Search
//
// # Tire
#include <cstdio>
#define INF = 0x3f3f3f3f
#define ll = long long

int t, n;
char input[100001], trie[100001][4];

int main()
{
    while ( ~scanf("%d", &t) )
        while ( t-- )
        {
            char ch;
            while ( scanf("%[^\n]", &ch) )
            {
            }
            getchar();
            scanf("%d", &n);
            while ( n-- )
            {
                scanf("%s", str);
            }
        }

    return 0;
}

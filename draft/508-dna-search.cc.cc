//
// 508 - DNA Search
//
// # Tire
#include <stdio.h>
//#include <iostream>
//#include <vector>
//#include <string>
//#include <algorithm>
//#include <cmath>
//#include <unoredered_map>
#define INF = 0x3f3f3f3f
#define ll = long long
//using namespace std;
//std::ios::sync_with_stdio(false), std::cin.tie(0);

int t;
const int N = 100005;
char s[N][4];

void buildTire()
{
    for (int i=1; i<N; ++i)
    {
        scanf("%s", s + 1);
        int u = 1;
        for (int j = 1; s[j]; ++j)
    }

}

int main()
{
    buildTire();
    while ( ~scanf("%d", t) )
        while ( t-- )

    return 0;
}

//
/* 272 - Smart Customer */
//
#include <cstdio>

int dp[11][11];

int main ()
{
    float p;
    int c = 1, t;
    while ( ~scanf("%f%d", &p, &t) )
    {
        while ( t-- )
        {
            scnaf("%d%f", &p, &n );
        }
        printf("Case %d\n:", c++);
        printf("Buy for $%.2f\n" , p);

    }
    return 0;
}

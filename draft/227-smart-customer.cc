// 272 - Smart Customer
//
//
#include <stdio.h>

float p;
int m;

int main ()
{
    int c = 1;
    while ( scanf("%f", &p) != EOF )
    {
        scanf("%d", &m);
        printf("Case %d\n:", c++);

        while ( m-- )
        {
        }

        printf("Buy for $%.2f\n" , p);

    }
    return 0;
}

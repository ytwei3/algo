#include <cstdio>
#define MAX_SIZE 1001

int main()
{
	char c;
	int n, m, a, b, ar[MAX_SIZE];
	scanf("%d%d", &n, &m);
	for (int i=1; i<=n; i++)
	while (n--) {
		scanf(" %c%d%d", &c, &a, &b);
		if (c == 'E') {

		}
	}
	return 0;
}